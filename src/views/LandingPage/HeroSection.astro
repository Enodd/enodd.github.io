<section
  id={"heroSection"}
  class="py-10 px-0 flex flex-col justify-center items-center"
>
  <div class="flex flex-row justify-center min-h-screen">
    <div>
      <!-- two images here -->
    </div>
    <div class="flex flex-col gap-3">
      <h1 class="text-4xl">
        Hello there <span id="hand" class="text-4xl">👋</span>
      </h1>
      <p>
        I'm Damian, and I'm front-end developer based in
        <a href="#" class="font-bold text-primary">Gliwice</a>
        <br />
        that focuses on building responsive and eye-catching front-ends with attention
        to detail.
      </p>
      <p>
        Currently studying computer science at
        <a href="#" class="font-bold text-primary">University Of Silesia</a>.
      </p>
      <p>I'm proficient at using React and can quickly learn new things.</p>
    </div>
  </div>
</section>

<style>
  @keyframes wave {
    0% {
      transform: rotate(5deg);
    }
    50% {
      transform: rotate(-5deg);
    }
    100% {
      transform: rotate(0deg);
    }
  }
  h1 {
    position: relative;
    width: fit-content;
  }
  #hand {
    position: absolute;
    transform-origin: right bottom;
  }
  h1:hover #hand {
    animation: 0.5s wave ease-in-out;
  }
  a {
    position: relative;
  }
  a::after {
    content: "";
    position: absolute;
    bottom: -2px;
    left: 0;
    width: 100%;
    height: 2px;
    transform: scaleX(0);
    transition: 0.3s ease-in-out;
    @apply bg-secondary;
  }
  a:hover::after {
    transform: scaleX(1);
  }
</style>
